Adjutant.Views.ContextMap = Backbone.View.extend({
  // initialize: function() {
  //   this.context = this.options.context;
  //   this.context.bind("change", this.render);
  //   this.render();
  // },
  // 
  // render: function() {
  //   $('#identities_contents').html(JST['identities/show']({context: this.context.models}));
  // }
});